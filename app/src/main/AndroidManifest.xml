<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guest.cofeshop">

    <!-- <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CoffeeShopsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/NoActionBarTheme">

            <!-- <meta-data -->
            <!-- android:name="com.google.android.geo.API_KEY"/> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CoffeeShopsActivity" />
        <activity android:name=".DisplayListActivity" />
        <activity android:name=".ui.CoffeeShopsDetailActivity" />
        <activity android:name=".ui.SavedCoffeeShopsListActivity"></activity>
    </application>

</manifest>